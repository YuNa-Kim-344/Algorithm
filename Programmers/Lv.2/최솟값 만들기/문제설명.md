# 최솟값 만들기

## 문제 설명

길이가 같은 배열 `A`, `B` 두 개가 있습니다.  
각 배열은 자연수로 이루어져 있습니다.

배열 `A`, `B`에서 각각 한 개의 숫자를 뽑아 두 수를 곱합니다.  
이러한 과정을 배열의 길이만큼 반복하며, 두 수를 곱한 값을 누적하여 더합니다.

이때 최종적으로 누적된 값이 최소가 되도록 만드는 것이 목표입니다.

단, 각 배열에서 `k`번째 숫자를 뽑았다면 다음에는 `k`번째 숫자를 다시 뽑을 수 없습니다.

---

## 예시

예를 들어 다음과 같은 배열이 주어졌다고 가정합니다.

```text
A = [1, 4, 2]
B = [5, 4, 4]
```

다음과 같이 숫자를 뽑아 곱한 값을 누적할 수 있습니다.

| 선택한 A의 값 | 선택한 B의 값 | 곱한 값 | 누적 값 |
|---|---:|---:|---:|
| 1 | 5 | 1 × 5 = 5 | 5 |
| 4 | 4 | 4 × 4 = 16 | 21 |
| 2 | 4 | 2 × 4 = 8 | 29 |

따라서 이 경우 최솟값은 `29`입니다.

---

## 제한사항

- 배열 `A`, `B`의 크기: `1,000` 이하의 자연수
- 배열 `A`, `B`의 원소의 크기: `1,000` 이하의 자연수

---

## 입출력 예

| A | B | answer |
|---|---|---:|
| `[1, 4, 2]` | `[5, 4, 4]` | `29` |
| `[1, 2]` | `[3, 4]` | `10` |

---

## 입출력 예 설명

### 입출력 예 #1

문제의 예시와 같습니다.

```text
A = [1, 4, 2]
B = [5, 4, 4]
```

최종적으로 만들 수 있는 최소 누적값은 `29`입니다.

---

### 입출력 예 #2

```text
A = [1, 2]
B = [3, 4]
```

다음과 같이 숫자를 선택할 수 있습니다.

| 선택한 A의 값 | 선택한 B의 값 | 곱한 값 | 누적 값 |
|---|---:|---:|---:|
| 1 | 4 | 1 × 4 = 4 | 4 |
| 2 | 3 | 2 × 3 = 6 | 10 |

따라서 최솟값은 `10`입니다.

---

## 함수 설명

`solution` 함수는 배열 `A`, `B`를 매개변수로 받아 최종적으로 만들 수 있는 최소 누적값을 반환해야 합니다.

```python
def solution(A, B):
    answer = 0
    return answer
```
